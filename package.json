{
  "name": "@labourchain/core-protocols",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic core chain protocols for LabourChain",
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "license": "MIT",
  "exports": {
    ".": "./lib/index.js",
    "./protocol": "./lib/protocol.js",
    "./record": "./lib/record.js",
    "./entity": "./lib/entity.js",
    "./block": "./lib/block.js"
  },
  "files": [
    "lib/**/*.js",
    "lib/**/*.d.ts"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:artifacts": "pnpm run build && pnpm --filter @labourchain/protocol-dev-sdk build && node scripts/build-core-artifacts.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "verify:release": "node scripts/verify-release-artifacts.mjs",
    "check": "pnpm run typecheck && pnpm run test && pnpm run build && pnpm --filter @labourchain/protocol-dev-sdk check && node scripts/build-core-artifacts.mjs && node scripts/smoke-package-exports.mjs && pnpm run verify:release"
  },
  "devDependencies": {
    "@deepseek-ai/cordis": "4.0.2",
    "@types/node": "^22.20.0",
    "esbuild": "0.28.2",
    "typescript": "6.0.3",
    "vitest": "^4.1.10"
  }
}
